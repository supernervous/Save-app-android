<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools" tools:context=".MainActivity">

s
    <item android:id="@+id/menu_edit" android:title="@string/action_edit"
        android:orderInCategory="100"
        app:showAsAction="always"
        android:visible="true"
        android:icon="@drawable/ic_mode_edit_teal"

        />

    <item android:id="@+id/menu_delete" android:title="@string/action_remove"
        android:orderInCategory="100"
        app:showAsAction="always"
        android:visible="true"
        android:icon="@drawable/ic_delete_teal"
        />

</menu>
